# Vitis / MicroBlaze V Software

Management application for AXI register setup, status monitoring, interrupt handling,
event FIFO logging, Sensor Guard configuration, and UART terminal output.

- `redundant_link_app/src/`: application source
- `hardware/`: XSA used to create the platform
- Build cache and IDE workspace logs are excluded.
